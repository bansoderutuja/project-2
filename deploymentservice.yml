apiVersion: apps/v1
kind: Deployment # Kubernetes resource kind we are creating
metadata:
  name: banking
spec:
  selector:
    matchLabels:
      app: banking-app
  replicas: 2 # Number of replicas that will be created for this deployment
  template:
    metadata:
      labels:
        app: banking-app
    spec:
      containers:
        - name: container
          image: rutu1304/project-2:latest # Image that will be used to containers in the cluster
          imagePullPolicy: Always # Fix: use Always if you want latest to actually be latest
          ports:
            - containerPort: 8070 # The port that the container is running on in the cluster


---

apiVersion: v1 # Kubernetes API version
kind: Service # Kubernetes resource kind we are creating
metadata: # Metadata of the resource kind we are creating
  name: banking-app
spec:
  selector:
    app: banking-app
  ports:
    - protocol: "TCP"
      port: 8070 # The port that the service is running on in the cluster
      targetPort: 8070 # The port exposed by the service
  type: LoadBalancer # type of the service.
